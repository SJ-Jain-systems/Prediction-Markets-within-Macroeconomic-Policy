.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# packaging / build artifacts (from `pip install -e .`)
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# raw/processed data pulls -- keep the folder, not the (often large, re-derivable) contents
data/*
!data/.gitkeep
